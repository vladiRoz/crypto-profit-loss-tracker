package crypro.profit.loss.tracker.receivers

import android.content.BroadcastReceiver
import android.content.Context
import android.content.Intent

class CoinAlarmReceiver : BroadcastReceiver() {

    companion object {
//        val EXTRA_DETAILS = "extra_details"
    }

    override fun onReceive(context: Context, intent: Intent) {
//        Log.i("CoinAlarmReceiver", "CoinAlarmReceiver")
//        val bytes = intent.getByteArrayExtra(EXTRA_DETAILS)
//        val serviceIntent = Intent(context, AlarmService::class.java)
//        serviceIntent.putExtra(EXTRA_DETAILS, bytes)
    }
}
